<Page x:Class="TaigerDesktop.Pages.CheckStat"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="750" d:DesignWidth="1100"
      Title="Статистика пользователей"
      Background="{StaticResource LightBackgroundBrush}">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="..\Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Статистика регистраций за 30 дней"
                   Style="{StaticResource PageTitleStyle}"
                   Grid.Row="0"
                   Margin="0 0 0 25"/>

        <!-- KPI Карточки -->
        <ItemsControl Grid.Row="1" Margin="0 0 0 30" ItemsSource="{Binding KpiCards}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="4"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource WhiteBrush}"
                            CornerRadius="16"
                            Padding="20"
                            Margin="8">
                        <Border.Effect>
                            <DropShadowEffect ShadowDepth="2" Opacity="0.15" BlurRadius="10"/>
                        </Border.Effect>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="{Binding Label}"
                                       FontSize="14"
                                       Foreground="{StaticResource MutedTextBrush}"/>
                            <TextBlock Text="{Binding Value}"
                                       FontSize="28"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource DarkPurpleBrush}"
                                       Margin="0 6 0 0"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- График -->
        <Border Grid.Row="2"
                Background="{StaticResource WhiteBrush}"
                CornerRadius="16"
                Padding="25">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="3" Opacity="0.12" BlurRadius="12"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Динамика новых пользователей"
                           FontSize="18"
                           FontWeight="Medium"
                           Foreground="{StaticResource DarkPurpleBrush}"
                           Margin="0 0 0 20"/>
                <Canvas x:Name="ChartCanvas"
                        Grid.Row="1"
                        Height="300"
                        Background="Transparent"
                        MouseMove="ChartCanvas_MouseMove"
                        SizeChanged="ChartCanvas_SizeChanged">
                    <Canvas x:Name="GridCanvas"/>
                    <Path x:Name="ChartPath"
                          Stroke="{StaticResource PrimaryBrush}"
                          StrokeThickness="4"
                          StrokeLineJoin="Round"
                          Fill="Transparent"/>
                    <Canvas x:Name="PointsCanvas"/>
                    <Border x:Name="TooltipBorder"
                            Background="{StaticResource WhiteBrush}"
                            CornerRadius="8"
                            Padding="12"
                            Visibility="Collapsed">
                        <TextBlock x:Name="TooltipText"
                                   Foreground="{StaticResource DarkPurpleBrush}"
                                   FontWeight="SemiBold"/>
                    </Border>
                </Canvas>
            </Grid>
        </Border>
    </Grid>
</Page>